/**
 * config.js — preencha antes de publicar.
 * Instruções passo a passo no README.md.
 */

export const FIREBASE = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  appId: '',
};

/** URL do seu Cloudflare Worker. Deixe vazio para desligar a IA. */
export const URL_WORKER = '';

/** Com true, o app funciona só com dados locais. Bom para testar sem Firebase. */
export const MODO_LOCAL = true;
